[Unit]
Description=Droid mount for /system_root
Before=local-fs.target systemd-modules-load.service

[Mount]
What=/dev/mapper/system
Where=/system_root
Type=ext4
Options=ro,barrier=1,discard
TimeoutSec=10

[Install]
WantedBy=local-fs.target
