[Service]
# Clear any hwcomposer-era ExecStartPre entries (dummy_compositor not needed with Mesa/KMS).
# setgid removal for this binary is handled by tmpfiles.d/lipstick-nosetgid.conf at system boot.
ExecStartPre=
LimitNOFILE=65536
