[Service]
# Base unit has Restart=always. On this port devicelock_in_lockscreen=false,
# so lipstick never calls the security UI for unlock — the service has nothing
# to do. Prevent restart loops if it exits unexpectedly.
Restart=no
