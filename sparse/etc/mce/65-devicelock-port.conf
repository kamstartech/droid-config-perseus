# Perseus port: no LUKS encrypted home (/dev/sailfish/home does not exist).
# encsfa cannot verify home encryption so it refuses all unlock requests.
# Setting devicelock_in_lockscreen=false lets MCE handle swipe-to-unlock
# directly without requiring devicelock daemon approval.
/system/osso/dsm/locks/devicelock_in_lockscreen=false
