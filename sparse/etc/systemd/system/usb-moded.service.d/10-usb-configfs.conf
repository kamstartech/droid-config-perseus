[Unit]
After=droid-hal-init.service config.mount
Requires=config.mount

[Service]
ExecStartPre=-/bin/sh -c 'if mountpoint -q /config 2>/dev/null && ! mountpoint -q /sys/kernel/config 2>/dev/null; then mkdir -p /sys/kernel/config && mount --bind /config /sys/kernel/config; fi'
